// Firebase is disabled for this version.
// These exports are kept as mocks/nulls to prevent import errors in other files
// if they haven't been fully cleaned up yet.

export const db = null;
export const storage = null;
export const auth = null;